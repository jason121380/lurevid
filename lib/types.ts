export type StoryboardScene = {
  sceneNumber: number;
  title: string;
  visualGoal: string;
  seedancePrompt: string;
};

export type ProjectSettings = {
  ratio: string;
  resolution: string;
  duration: number;
};
